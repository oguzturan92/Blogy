@model List<Tag>
@{
    ViewData["Title"] = "Etiket Listesi";
    Layout = "_LayoutAdmin";
}

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">ETİKET LİSTESİ</h4>
                    <div class="table-responsive">
                        <table class="table mb-3">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Ad</th>
                                    <th colspan="2"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach(var item in Model)
                                {
                                    <tr>
                                        <td>@item.TagId</td>
                                        <td>@item.TagTitle</td>
                                        <td><a asp-area="Admin" asp-controller="Tag" asp-action="TagUpdate" asp-route-id="@item.TagId" class="btn btn-success">Güncelle</a></td>
                                        <td><a asp-area="Admin" asp-controller="Tag" asp-action="TagDelete" asp-route-id="@item.TagId" class="btn btn-danger">Sil</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a asp-area="Admin" asp-controller="Tag" asp-action="TagCreate" class="btn btn-primary">Yeni Etiket Girişi</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>