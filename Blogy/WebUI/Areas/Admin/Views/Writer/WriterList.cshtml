@model List<AppUser>
@{
    ViewData["Title"] = "Kullanıcı Listesi";
    Layout = "_LayoutAdmin";
}

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">KULLANICI LİSTESİ</h4>
                    <div class="table-responsive">
                        <table class="table mb-3">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Ad</th>
                                    <th colspan="2"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach(var item in Model)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Name @item.Surname</td>
                                        <td><a asp-area="Admin" asp-controller="Writer" asp-action="WriterUpdate" asp-route-id="@item.Id" class="btn btn-success">Güncelle</a></td>
                                        <td><a asp-area="Admin" asp-controller="Writer" asp-action="WriterDelete" asp-route-id="@item.Id" class="btn btn-danger">Sil</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a asp-area="Admin" asp-controller="Writer" asp-action="WriterCreate" class="btn btn-primary">Yeni Kullanıcı Girişi</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>